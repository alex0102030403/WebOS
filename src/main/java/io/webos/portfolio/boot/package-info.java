/**
 * Boot subsystem - Stateless configuration provider for OS initialization.
 * 
 * Provides boot configuration including OS version, theme, wallpaper, and user identity.
 * All users are treated as "Visitor" without authentication requirements.
 */
package io.webos.portfolio.boot;
