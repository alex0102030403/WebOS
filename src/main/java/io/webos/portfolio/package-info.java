/**
 * WebOS Portfolio - A browser-based Windows 11 simulation powered by a Java 25 backend Kernel.
 * 
 * This package serves as the root for the backend kernel, implementing four core subsystems:
 * boot, filesystem, processes, and terminal. Each subsystem follows the BCE (Boundary-Control-Entity)
 * architectural pattern for clean separation of concerns.
 */
package io.webos.portfolio;
