/**
 * Browser proxy subsystem for the WebOS portfolio.
 * 
 * Provides secure, whitelist-based proxying of external web content.
 * Fetches HTML from approved URLs, sanitizes dangerous elements,
 * rewrites relative URLs, and injects custom styles for display
 * within the WebOS window environment.
 */
package io.webos.portfolio.browser;
